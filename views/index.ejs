<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <meta http-equiv="X-UA-Compatible" content="ie=edge" />
      <link rel="stylesheet" href="/style.css" />
      <link
         href="https://fonts.googleapis.com/css?family=Lato|Play|Roboto&display=swap"
         rel="stylesheet"
      />
      <title>Sentimental</title>
   </head>
   <body>
      <h1><%= heading %></h1>
      <form action="/app" method="post">
         <textarea
            name="input"
            id=""
            placeholder="Enter your text here..."
         ></textarea>
         <button type="submit">Analyze</button>
      </form>

      <% if(!data) { %> <% } else{ %>
      <section>
         <div>
            <span class="heading">Status : </span>
            <p class="values">
               <%= (data.score < 0) ? "negative" : (data.score==0 ? "neutral" :
               "positive") %>
            </p>
         </div>
         <div>
            <span class="heading">Score : </span>
            <p class="values"><%= data.score ? data.score : 0 %></p>
         </div>
         <div>
            <span class="heading">Positive words : </span>
            <p class="values">
               <%= data.positive ? data.positive.join(', ') : '' %>
            </p>
         </div>
         <div>
            <span class="heading">Negative words : </span>
            <p class="values">
               <%= data.negative ? data.negative.join(', ') : '' %>
            </p>
         </div>
         <!-- <div>
                  <span class="heading">Alternative words : </span>
                  <p class="values">bad,really bad,sucks,shit,useless,boring</p>
               </div> -->
      </section>
      <% } %>
   </body>
</html>
